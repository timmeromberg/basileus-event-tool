# Run Event Tool

Build and launch the event visualization tool.

```bash
cd basileus-event-tool && ./gradlew run
```

The event tool reads from `basileus-content/events/` and displays event relationships via outcomes.